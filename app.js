
var app = angular.module('chatiNG', ['pubNub']);

//app.config(function ($routeProvider) {
//    $routeProvider.
//      when('/', { controller: 'chatCtrl', templateUrl: 'chat.html' }).
//      otherwise({ redirectTo: '/' });
//});